<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.emdata.ivvs.dao.FtpDataConfigMapper">

    <select id="queryLastestConfig" resultType="com.emdata.ivvs.vo.RuleVo" parameterType="com.emdata.ivvs.vo.RuleVo">
        select * from ftp_data_config
        <!-- WHERE fid=#{fid} -->
    </select>

</mapper>
